---
import Base from '../layouts/Base.astro';
import NotFound from '../components/library/NotFound.svelte';

export const prerender = true;
---

<Base title="404 — Pressroom">
  <main class="spread spread--ink nf-shell">
    <NotFound client:only="svelte" />
  </main>
</Base>

<style>
  .nf-shell {
    position: relative;
    z-index: 1;
  }
</style>
